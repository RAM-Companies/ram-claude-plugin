{
  "skill_name": "create-edge-function",
  "evals": [
    {
      "id": 0,
      "prompt": "Add a new Edge Function called export-quote-pdf that lets a logged-in admin generate a signed download URL for a quote's PDF. Only admins should be able to call it.",
      "expected_output": "A new supabase/functions/export-quote-pdf/ with index.ts (Deno.serve handler, verify_jwt left at gateway default true, extracts token via extractToken-style helper, uses service-role client + auth.getUser, checks admin role server-side, calls CORS helper, handles OPTIONS), _lib.ts (pure validation/logic, no Supabase client), index.test.ts (Deno tests against _lib.ts only), and a config.toml entry for the function."
    },
    {
      "id": 1,
      "prompt": "I need a new Edge Function called inventory-sync that a partner's server calls directly with a shared secret header — there's no user login involved, similar to how create-quote-from-mac works in this repo.",
      "expected_output": "A new supabase/functions/inventory-sync/ built on Hono + @hono/zod-openapi (external-facing, per Step 3), with Zod schemas defining request/response shape, an OpenAPI spec route, verify_jwt = false in config.toml with an inline comment explaining why, a constant-time shared-secret check as the first thing that runs on every non-preflight path, pure logic split into _lib.ts, index.test.ts testing only the pure logic, and — since inventory_items doesn't exist yet — an explicit call-out naming `supabase migration new inventory_items` rather than a buried comment."
    }
  ]
}
