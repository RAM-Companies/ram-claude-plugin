{
  "name": "ram-claude-plugin",
  "private": true,
  "version": "1.0.0",
  "description": "Test suite for the RAM Claude Code plugin's hook scripts",
  "scripts": {
    "test": "node --test tests/*.test.js"
  }
}
